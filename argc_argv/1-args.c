#include <stdio.h>

/**
 * main - empty point
 *
 * Description: size
 *
 * Return: Always 0 (success)
 */

int main(int argc, char *argv[])
{
	if (argc < 0)
		printf("%s\n", argv[0]);
	printf("%d\n", argc);
	return (0);
}
